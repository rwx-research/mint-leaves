- key: $LEAF_NAME--publish-production
  if: ${{ init.publishleaves }}
  after: [$LEAF_TEST_TASKS]
  run: |
    curl \
      --request POST \
      --fail-with-body \
      --header "Authorization: Bearer $RWX_ACCESS_TOKEN" \
      --header 'Accept: application/json' \
      --header 'Content-Encoding: application/json' \
      --data '{ "digest": "$LEAF_DIGEST" }' \
      https://cloud.rwx.com/mint/api/leaves/publish
  env:
    RWX_ACCESS_TOKEN: ${{ vaults.mint_leaves_development.secrets.RWX_ACCESS_TOKEN_PRODUCTION_MINT }}
- key: $LEAF_NAME--publish-staging
  if: ${{ init.publishleaves }}
  # we publish production first because we test using production Mint
  # publishing to staging is more about replication so leaves are available there too
  after: $LEAF_NAME--publish-production
  run: |
    curl \
      --request POST \
      --fail-with-body \
      --header "Authorization: Bearer $RWX_ACCESS_TOKEN" \
      --header 'Accept: application/json' \
      --header 'Content-Encoding: application/json' \
      --data '{ "digest": "$LEAF_DIGEST" }' \
      https://staging.cloud.rwx.com/mint/api/leaves/publish
  env:
    RWX_ACCESS_TOKEN: ${{ vaults.mint_leaves_development.secrets.RWX_ACCESS_TOKEN_STAGING_MINT }}
